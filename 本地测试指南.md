# 🚀 本地测试定时同步功能 - 快速开始

## 📋 前置准备

### 1. Python环境
确保已安装 Python 3.8 或更高版本：
```bash
python --version
```

### 2. 获取歌单ID

1. 打开浏览器访问 [网易云音乐](https://music.163.com/)
2. 登录你的账号
3. 进入你想要测试的歌单页面
4. 从URL中复制歌单ID

例如：
```
https://music.163.com/#/playlist?id=1234567890
                                         ^^^^^^^^^^
                                         这就是歌单ID
```

---

## 🔧 配置步骤

### 方式一：使用自动化脚本（推荐）

双击运行 `test_sync.bat`，它会自动：
- ✅ 升级pip
- ✅ 安装依赖包
- ✅ 检查必要文件
- ✅ 提示配置
- ✅ 启动服务

### 方式二：手动配置

#### 步骤1：安装依赖

```bash
# 升级pip
python -m pip install --upgrade pip

# 安装依赖（使用清华镜像源加速）
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

#### 步骤2：获取Cookie

**方法A：二维码登录（推荐）**
```bash
python qr_login.py
```
然后用网易云音乐APP扫码登录。

**方法B：手动复制Cookie**
1. 浏览器打开网易云音乐并登录
2. 按F12打开开发者工具
3. 切换到 Network 标签
4. 刷新页面
5. 找到任意请求，查看 Request Headers
6. 复制 Cookie 字段的全部内容
7. 粘贴到 `cookie.txt` 文件中

#### 步骤3：配置.env文件

编辑 `.env` 文件，设置以下参数：

```ini
# 启用定时同步
ENABLE_SYNC=true

# 填入你的歌单ID（必填！）
PLAYLIST_IDS=你的歌单ID

# 测试时设置为较短间隔（60秒）
SYNC_INTERVAL=60

# 音质选择
SYNC_QUALITY=lossless
```

**示例：**
```ini
ENABLE_SYNC=true
PLAYLIST_IDS=1234567890,9876543210
SYNC_INTERVAL=60
SYNC_QUALITY=lossless
```

---

## 🧪 运行测试

### 方法1：使用测试脚本

```bash
python test_sync_local.py
```

这个脚本会：
- ✅ 检查所有依赖
- ✅ 验证配置文件
- ✅ 进行快速功能测试
- ✅ 询问是否启动服务

### 方法2：直接启动服务

```bash
python main.py
```

---

## 📊 验证功能

### 1. 查看启动信息

服务启动后，你应该看到类似输出：

```
============================================================
🚀 网易云音乐API服务启动中...
============================================================
📡 服务地址: http://0.0.0.0:5000
📁 下载目录: D:\code_projects\Netease_url\downloads
📋 日志级别: INFO

⏰ 定时同步服务:
  ├─ 启用状态: ✓ 已启用
  ├─ 歌单数量: 1
  ├─ 同步音质: lossless
  └─ 同步间隔: 60秒

📚 API端点:
  ├─ GET  /health        - 健康检查
  ├─ POST /song          - 获取歌曲信息
  ├─ GET  /sync/status   - 同步状态
  ├─ POST /sync/now      - 立即同步
  └─ ...

⏰ 启动时间: 2025-06-07 15:30:00
🌟 服务已就绪，等待请求...

============================================================
playlist_sync - INFO - 开始同步 1 个歌单
============================================================
```

### 2. 测试API接口

在浏览器中访问或使用curl：

**查看同步状态：**
```
http://localhost:5000/sync/status
```

**立即执行同步：**
```
http://localhost:5000/sync/now
```

**使用curl测试：**
```bash
# 查看状态
curl http://localhost:5000/sync/status

# 触发同步
curl -X POST http://localhost:5000/sync/now
```

### 3. 查看日志

**实时日志：**
服务运行时会在控制台显示实时日志。

**日志文件：**
- `playlist_sync.log` - 同步专用日志
- `music_api.log` - API服务日志

**查看日志内容：**
```bash
# PowerShell
Get-Content playlist_sync.log -Wait

# 或使用记事本打开
notepad playlist_sync.log
```

### 4. 检查下载的文件

同步的歌曲会保存在 `downloads` 目录：

```bash
# 查看下载的文件
dir downloads

# 或在资源管理器中打开
explorer downloads
```

---

## ❓ 常见问题

### Q1: 提示缺少依赖包？

```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2: cffi编译失败？

这是Windows常见问题，解决方法：

**方案A：使用预编译包**
```bash
pip install cffi --only-binary :all:
```

**方案B：安装Visual C++ Build Tools**
下载：https://visualstudio.microsoft.com/visual-cpp-build-tools/

**方案C：直接使用Docker（推荐）**
```bash
docker-compose build
docker-compose up -d
```

### Q3: Cookie无效？

1. 确认Cookie格式正确
2. 确认包含 `MUSIC_U` 字段
3. Cookie可能过期，重新获取

测试Cookie有效性：
```bash
curl http://localhost:5000/health
```

查看响应中的 `cookie_status` 字段。

### Q4: 没有开始同步？

检查清单：
- ✅ `ENABLE_SYNC=true`
- ✅ `PLAYLIST_IDS` 已填写
- ✅ Cookie有效
- ✅ 查看日志是否有错误

### Q5: 如何停止服务？

按 `Ctrl+C` 停止服务。

### Q6: 想修改同步间隔？

编辑 `.env` 文件，修改 `SYNC_INTERVAL`，然后重启服务。

测试时建议设置为 `60`（60秒），方便观察效果。

---

## 🎯 完整测试流程示例

```bash
# 1. 安装依赖
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 2. 二维码登录获取Cookie
python qr_login.py

# 3. 编辑 .env 文件，填入歌单ID
notepad .env

# 4. 启动服务
python main.py

# 5. 等待60秒，观察日志输出
# 或者立即触发同步
# 浏览器访问: http://localhost:5000/sync/now

# 6. 查看下载的文件
explorer downloads

# 7. 查看同步日志
notepad playlist_sync.log
```

---

## 🐛 调试技巧

### 1. 启用调试模式

编辑 `.env`：
```ini
DEBUG=true
LOG_LEVEL=DEBUG
```

### 2. 单独测试同步模块

```python
python -c "from playlist_sync import PlaylistSyncConfig; print('OK')"
```

### 3. 测试Cookie有效性

```python
python -c "from cookie_manager import CookieManager; cm = CookieManager(); print(cm.is_cookie_valid())"
```

### 4. 测试单个歌单同步

创建测试脚本 `test_single_sync.py`：
```python
from playlist_sync import PlaylistSyncConfig, PlaylistSyncService

config = PlaylistSyncConfig(
    playlist_ids=['你的歌单ID'],
    quality='lossless',
    sync_interval=60
)

service = PlaylistSyncService(config)
result = service.sync_single_playlist('你的歌单ID')
print(result)
```

---

## 📝 下一步

测试成功后，你可以：

1. **调整同步间隔**：改为更合理的值（如3600秒=1小时）
2. **添加更多歌单**：在 `PLAYLIST_IDS` 中添加多个ID
3. **部署到Docker**：使用 `docker-compose up -d` 部署到生产环境
4. **配置Cron表达式**：设置特定时间同步（如每天凌晨2点）

---

## 💡 提示

- 🎵 首次同步会下载所有歌曲，可能需要较长时间
- 💾 无损音质每首歌约20-30MB，注意存储空间
- 🔄 系统会自动跳过已存在的歌曲，不会重复下载
- 📊 同步历史保存在 `downloads/sync_history.json`

祝你测试顺利！如有问题请查看日志文件。
