# 定时歌单同步功能使用说明

## 🎯 功能介绍

定时歌单同步功能可以自动将指定的网易云音乐歌单中的歌曲下载到本地，支持：
- ✅ 定时自动同步（支持固定间隔和Cron表达式）
- ✅ 多歌单同时同步
- ✅ 智能去重（只下载新歌）
- ✅ 多种音质选择
- ✅ 详细的同步日志
- ✅ 同步历史记录

## 📝 配置步骤

### 1. 获取歌单ID

1. 打开网易云音乐网页版 (https://music.163.com/)
2. 进入你想要同步的歌单页面
3. 从URL中获取歌单ID

例如：
```
https://music.163.com/#/playlist?id=1234567890
                                         ^^^^^^^^^^
                                         这就是歌单ID
```

### 2. 修改 docker-compose.yml

编辑 `docker-compose.yml` 文件，填入你的配置：

```yaml
environment:
  # 启用定时同步
  - ENABLE_SYNC=true
  # 歌单ID列表（多个用逗号分隔）
  - PLAYLIST_IDS=1234567890,9876543210
  # 同步音质
  - SYNC_QUALITY=lossless
  # 同步间隔（秒）
  - SYNC_INTERVAL=3600
  # 或使用Cron表达式（可选）
  # - CRON_EXPRESSION=0 2 * * *
```

**重要：** 记得把 `COOKIE` 改成你实际的Cookie值！

### 3. 启动服务

```bash
# 重新构建镜像（因为添加了新依赖）
docker-compose build

# 启动服务
docker-compose up -d

# 查看日志
docker logs -f netease-music-downloader
```

## ⚙️ 配置项说明

### ENABLE_SYNC
- **类型**: boolean
- **默认**: false
- **说明**: 是否启用定时同步功能

### PLAYLIST_IDS
- **类型**: string (逗号分隔)
- **默认**: 空
- **说明**: 需要同步的歌单ID列表
- **示例**: `PLAYLIST_IDS=1234567890,9876543210,111222333`

### SYNC_QUALITY
- **类型**: string
- **默认**: lossless
- **可选值**: 
  - `standard` - 标准音质 (~3-5 MB)
  - `exhigh` - 极高音质 (~8-12 MB)
  - `lossless` - 无损音质 (~20-30 MB) ⭐推荐
  - `hires` - Hi-Res音质 (~50-100 MB)
  - `sky` - 沉浸环绕声 (~30-40 MB)
  - `jyeffect` - 高清环绕声 (~25-35 MB)
  - `jymaster` - 超清母带 (~50-80 MB)

### SYNC_INTERVAL
- **类型**: integer
- **默认**: 3600 (1小时)
- **说明**: 同步间隔时间（秒）
- **示例**: 
  - `1800` = 30分钟
  - `3600` = 1小时
  - `7200` = 2小时

### CRON_EXPRESSION (可选)
- **类型**: string
- **默认**: 空
- **说明**: Cron表达式，如果设置则优先于SYNC_INTERVAL
- **示例**:
  - `*/30 * * * *` - 每30分钟执行
  - `0 * * * *` - 每小时整点执行
  - `0 */2 * * *` - 每2小时执行
  - `0 2 * * *` - 每天凌晨2点执行 ⭐推荐
  - `0 2 * * 0` - 每周日凌晨2点执行
  - `0 0 1 * *` - 每月1号凌晨0点执行

在线Cron表达式生成器：https://crontab.guru/

### DOWNLOADS_DIR
- **类型**: string
- **默认**: /app/downloads
- **说明**: Docker容器内的下载目录（不要修改）

## 🌐 API接口

### 1. 查看同步状态

```bash
GET http://localhost:5000/sync/status
```

响应示例：
```json
{
  "status": 200,
  "success": true,
  "message": "获取同步状态成功",
  "data": {
    "service_running": true,
    "job_count": 1,
    "config": {
      "playlist_ids": ["1234567890"],
      "quality": "lossless",
      "sync_interval": 3600,
      "download_dir": "/app/downloads"
    },
    "last_sync": "2025-06-07T10:30:00",
    "total_synced_songs": 156
  }
}
```

### 2. 立即执行同步

```bash
POST http://localhost:5000/sync/now
```

或在浏览器中访问：
```
http://localhost:5000/sync/now
```

响应示例：
```json
{
  "status": 200,
  "success": true,
  "message": "同步任务已启动",
  "data": {
    "message": "同步任务已在后台启动"
  }
}
```

### 3. 查看API信息

```bash
GET http://localhost:5000/api/info
```

## 📊 日志查看

### 查看实时日志

```bash
# 查看Docker容器日志
docker logs -f netease-music-downloader

# 查看最近的100行日志
docker logs --tail 100 netease-music-downloader
```

### 日志示例

```
2025-06-07 10:30:00 - playlist_sync - INFO - ============================================================
2025-06-07 10:30:00 - playlist_sync - INFO - 开始同步 2 个歌单
2025-06-07 10:30:00 - playlist_sync - INFO - ============================================================
2025-06-07 10:30:00 - playlist_sync - INFO - 
开始同步歌单: 1234567890
2025-06-07 10:30:01 - playlist_sync - INFO - 歌单名称: 我的最爱
2025-06-07 10:30:01 - playlist_sync - INFO - 歌曲总数: 100
2025-06-07 10:30:02 - playlist_sync - INFO - [1/100] 下载: 歌曲名 - 艺术家
2025-06-07 10:30:05 - playlist_sync - INFO - ✓ 下载成功: /app/downloads/艺术家 - 歌曲名.flac
2025-06-07 10:30:06 - playlist_sync - INFO - [2/100] 跳过已存在: 另一首歌 - 艺术家
...
2025-06-07 10:35:00 - playlist_sync - INFO - ============================================================
2025-06-07 10:35:00 - playlist_sync - INFO - 同步完成: 成功 2/2, 共下载 85 首歌曲
2025-06-07 10:35:00 - playlist_sync - INFO - ============================================================
```

## ❓ 常见问题

### Q1: 同步不生效？

**检查清单：**
1. ✅ 确认 `ENABLE_SYNC=true`
2. ✅ 确认 `PLAYLIST_IDS` 已填写正确的歌单ID
3. ✅ 确认 Cookie 有效且未过期
4. ✅ 查看日志是否有错误信息

```bash
# 查看详细日志
docker logs netease-music-downloader | grep -i error
```

### Q2: 如何修改同步频率？

调整 `SYNC_INTERVAL`（秒）或设置 `CRON_EXPRESSION`：

```yaml
# 方式1：每30分钟同步
- SYNC_INTERVAL=1800

# 方式2：使用Cron表达式（推荐）
- CRON_EXPRESSION=*/30 * * * *
```

修改后重启容器：
```bash
docker-compose up -d
```

### Q3: 如何添加/删除歌单？

修改 `PLAYLIST_IDS`，然后重启容器：

```yaml
# 添加新歌单
- PLAYLIST_IDS=1234567890,9876543210,111222333

# 删除某个歌单（移除对应的ID即可）
- PLAYLIST_IDS=1234567890
```

重启：
```bash
docker-compose up -d
```

### Q4: 歌曲下载到哪里了？

根据 `volumes` 配置，歌曲会下载到主机映射的目录：
```
/vol3/1000/音乐/网易云/Download
```

在容器中路径为：
```
/app/downloads
```

### Q5: 如何手动触发同步？

**方法1：使用curl命令**
```bash
curl -X POST http://localhost:5000/sync/now
```

**方法2：浏览器访问**
```
http://localhost:5000/sync/now
```

**方法3：使用Python脚本**
```python
import requests
response = requests.post('http://localhost:5000/sync/now')
print(response.json())
```

### Q6: Cookie过期了怎么办？

1. 重新登录网易云音乐获取新Cookie
2. 更新 `docker-compose.yml` 中的 `COOKIE` 值
3. 重启容器

```bash
docker-compose up -d
```

### Q7: 如何查看已同步的歌曲历史？

同步历史保存在下载目录的 `sync_history.json` 文件中：

```bash
cat /vol3/1000/音乐/网易云/Download/sync_history.json
```

或使用jq格式化查看：
```bash
cat /vol3/1000/音乐/网易云/Download/sync_history.json | jq
```

### Q8: 部分歌曲下载失败？

可能原因：
1. **版权限制** - 某些歌曲因版权无法下载
2. **VIP歌曲** - 需要会员权限
3. **网络问题** - 临时网络故障

解决方法：
- 查看日志确认具体错误
- 下次同步时会自动重试失败的歌曲
- 可以手动触发同步重试

### Q9: 如何停止定时同步？

**临时停止：**
修改 `.env` 或 `docker-compose.yml`：
```yaml
- ENABLE_SYNC=false
```

重启容器：
```bash
docker-compose up -d
```

**完全禁用：**
注释或删除相关配置项即可。

### Q10: 占用多少存储空间？

取决于音质选择和歌曲数量：

| 音质 | 单首大小 | 100首 | 500首 |
|------|---------|-------|-------|
| standard | ~3-5 MB | ~400 MB | ~2 GB |
| exhigh | ~8-12 MB | ~1 GB | ~5 GB |
| lossless | ~20-30 MB | ~2.5 GB | ~12.5 GB |
| hires | ~50-100 MB | ~7.5 GB | ~37.5 GB |

建议定期清理不需要的歌曲。

## ⚠️ 注意事项

1. **Cookie有效期**：确保Cookie有效，否则同步会失败
2. **版权限制**：部分歌曲可能因版权原因无法下载
3. **存储空间**：无损音质歌曲较大，请确保有足够存储空间
4. **网络带宽**：大量下载时注意网络带宽消耗
5. **合理使用**：请遵守网易云音乐的使用条款
6. **首次同步**：首次同步会下载所有歌曲，可能需要较长时间
7. **智能去重**：系统会自动跳过已存在的歌曲，不会重复下载

## 🛠️ 技术支持

如有问题，请查看以下日志文件：

- `playlist_sync.log` - 同步专用日志
- `music_api.log` - API服务日志
- Docker日志：`docker logs netease-music-downloader`

提交问题时请提供：
1. 相关日志内容
2. 配置文件（隐藏敏感信息）
3. 错误信息截图

---

**祝你使用愉快！🎵**
